--from : https://leetcode.com/problems/fix-names-in-a-table/


# Write your MySQL query statement below
SELECT user_id,CONCAT(upper(left(name,1)),LOWER(SUBSTRING(name,2))) AS NAME
FROM Users
ORDER BY user_id;
